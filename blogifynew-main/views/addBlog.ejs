<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('partials/head') %>
    <title>Write Your Story - Blogify</title>
  </head>
  <body>
    <%- include('partials/nav') %>
    
    <div class="container mt-4">
      <div class="row justify-content-center">
        <div class="col-lg-8">
          <div class="auth-card">
            <h2 class="auth-title">Write Your Story</h2>
            <p class="text-center text-muted mb-4">Share your thoughts, experiences, and ideas with the world.</p>
            
            <form action="/blog" method="post" enctype="multipart/form-data"> 
              <div class="mb-4">
                <label for="coverImage" class="form-label">Cover Image</label>
                <input
                  type="file"
                  class="form-control"
                  id="coverImage"
                  name="coverImage"
                  accept="image/*"
                  required
                />
                <div class="form-text">Choose a compelling image that represents your story.</div>
              </div>
              
              <div class="mb-4">
                <label for="title" class="form-label">Story Title</label>
                <input
                  type="text"
                  class="form-control"
                  id="title"
                  name="title"
                  placeholder="Enter your story title..."
                  required
                />
              </div>
              
              <div class="mb-4">
                <label for="body" class="form-label">Your Story</label>
                <textarea 
                  name="body"
                  class="form-control"
                  id="body"
                  rows="12"
                  placeholder="Write your story here..."
                  required
                ></textarea>
                <div class="form-text">Share your thoughts, experiences, or ideas. Be authentic and engaging.</div>
              </div>
              
              <%- include('partials/aiButtons') %>
              
              <div class="d-grid gap-2">
                <button type="submit" class="btn btn-primary btn-lg">Publish Story</button>
                <a href="/" class="btn btn-outline-secondary">Cancel</a>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>

    <%- include('./partials/footer') %>
    <%- include('./partials/scripts') %>
  </body>
</html>
